extends layout 

block content 
  h2 新規作成
  form(method="post" action="/posts") 
    input(type="hidden" name="_csrf" value!=csrfToken)
    div.form-group
      textarea(name="title" placeholder="タイトル" rows="1").form-control
      textarea(name="body" placeholder="本文" rows="4").form-control
    div.form-group
      button(type="submit").btn.btn-primary #[i.bi.bi-chat-heart-fill] 投稿